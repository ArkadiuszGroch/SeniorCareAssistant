<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/buttonsBackground</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/buttonsBackground</item>
    </style>

    <style name="buttons">
        <item name="android:background">@color/buttonsBackground</item>
        <item name="android:textColor">@color/buttonsText</item>
        <item name="android:layout_margin">5dp</item>
    </style>

</resources>
